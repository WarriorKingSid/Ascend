/**
 * Frontend configuration — update when connecting your backend.
 *
 * Using your Momen project and admin token from .momen/credentials.yaml.
 */
const API_CONFIG = {
  useMock: false,
  graphqlEndpoint: 'http://localhost:5501/graphql',
  authToken: '',
};
